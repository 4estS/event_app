<div class="w-screen h-screen flex items-center justify-center bg-slate-500">
  <div class="flex flex-col gap-4 max-w-md p-6 shadow rounded-xl bg-slate-200">
    <h1>Welcome!</h1>
    <p>Sign in, sign up or provide your location... it's that easy.
      <p>
        <div class="flex justify-center gap-4">
          <%= link_to "Sign In", sign_in_path, class: "px-4 py-2 bg-blue-600 text-white rounded-xl hover:bg-blue-700 transition" %>
          <%= link_to "Sign Up", sign_up_path, class: "px-4 py-2 bg-green-600 text-white rounded-xl hover:bg-green-700 transition" %>
        </div>
        <div class="my-8 flex items-center gap-4 before:h-px before:flex-1 before:bg-gray-300  before:content-[''] after:h-px after:flex-1 after:bg-gray-300  after:content-['']">OR</div>
        <h2 class="text-xl font-semibold mb-4">Enter Your Location</h2>
        <%= render "guest_location/form" %>
      </div>
    </div>
